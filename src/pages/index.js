import React, { useContext } from "react";

import MainLayout from "../layout/MainLayout";

const index = () => {
  return (
    <MainLayout>
      <div> hello</div>
    </MainLayout>
  );
};

export default index;
